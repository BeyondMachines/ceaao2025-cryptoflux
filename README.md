# CryptoFlux
The repo to build up the tools and deployment for the masterclass


###  Prerequisites
- Docker Desktop installed and running  


Make sure to copy `.env.prototype` to `.env` before running.
The stack starts with `setup.bat` (Windows) or `setup.sh` (Mac, Linux)